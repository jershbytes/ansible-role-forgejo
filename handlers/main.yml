---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart forgejo
  ansible.builtin.systemd:
    name: forgejo
    state: restarted

- name: Restart caddy
  ansible.builtin.systemd:
    name: caddy
    state: restarted
